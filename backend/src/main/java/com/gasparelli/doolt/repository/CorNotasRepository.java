package com.gasparelli.doolt.repository;

import com.gasparelli.doolt.model.CorNotasEntity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CorNotasRepository extends JpaRepository<CorNotasEntity, Long> {
}
